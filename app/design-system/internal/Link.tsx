/**
 * A simple re-export from react-router-dom.
 * This is used to avoid having to import react-router-dom in every file.
 *
 * Later on, if we need to support a different link component, like Next's link,
 * we can modify this to support some project-specific config or something.
 */
export { Link } from "react-router-dom";
